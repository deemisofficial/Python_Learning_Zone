print("enter first number")
n1 = input()
print("enter second number")
n2 = input()
print("the sum of these two numbers is", int(n1) + int(n2))

# In this snippet, we are taking two inputs from the user and adding them.
# We are using the input() function to take the input from the user.
# We are using the int() function to convert the string input into an integer.
# We are using the + operator to add the two integers.
10# We are using the print() function to print the output on the screen.
